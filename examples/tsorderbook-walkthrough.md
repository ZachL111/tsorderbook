# Tsorderbook Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | spread pressure | 156 | ship |
| stress | fill risk | 154 | ship |
| edge | portfolio drift | 201 | ship |
| recovery | quote width | 222 | ship |
| stale | spread pressure | 167 | ship |

Start with `recovery` and `stress`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `quote width` against `fill risk`, not the raw score alone.
